/**
 * Products / variants with stock strictly below this count are treated as low stock
 * (e.g. 0, 1, 2 when value is 3). Keep in sync with frontend `inventoryConstants.ts`.
 */
export const LOW_STOCK_ALERT_EXCLUSIVE_MAX = 3;
