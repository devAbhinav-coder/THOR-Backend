global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # House of Rani backend metrics (via OTel Collector Prometheus exporter)
  - job_name: "house-of-rani-backend"
    static_configs:
      - targets: ["otel-collector:8889"]
